import React from "react";
import PricebookSettings from "./PricebookSettings";

export default function Settings() {
  // Backward-compat: render the actual settings page
  return <PricebookSettings />;
}